openapi: 3.0.3
info:
  title: Fashion Store API
  version: 0.0.0
  description: >
    Formal contract, filled in per-endpoint as each module is implemented.
    See ../docs/api-spec.md for the current high-level endpoint inventory
    this will be generated from.
servers:
  - url: /api/v1
paths: {}
components:
  schemas: {}
